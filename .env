# EC2 only — copy to `.env` on each instance and use: `docker run ... --env-file .env`
# (Local development: use docker-compose.a2.yml without this file; compose sets its own defaults.)

# RDS / Aurora (writer endpoint, same DB name/user as CloudFormation stack)
MYSQL_HOST=bookstore-db-dev.cluster-c1i2eooes18j.us-east-1.rds.amazonaws.com
MYSQL_PORT=3306
MYSQL_DATABASE=bookstore
MYSQL_USER=bookuser
MYSQL_PASSWORD=jaibaba1008

# Book service — optional summaries
GEMINI_API_KEY=AIzaSyC5PgyUvaFxjhPEbAntsl-Mfl6zXRGE7d8
GEMINI_MODEL=gemini-1.5-flash
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# BFF — internal ALB DNS from AWS (listener port 3000)
URL_BASE_BACKEND_SERVICES=http://internal-bookstore-dev-InternalALB-1260139465.us-east-1.elb.amazonaws.com:3000
