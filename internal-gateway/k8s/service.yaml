apiVersion: v1
kind: Service
metadata:
  name: internal-gateway
  namespace: bookstore-ns
  labels:
    app: internal-gateway
spec:
  type: ClusterIP
  selector:
    app: internal-gateway
  ports:
    - name: http
      port: 3000
      targetPort: http
