apiVersion: v1
kind: Service
metadata:
  name: web-bff
  namespace: bookstore-ns
  labels:
    app: web-bff
spec:
  type: LoadBalancer
  selector:
    app: web-bff
  ports:
    - name: http
      port: 80
      targetPort: http
